import * as React from "react";
import { StyleSheet } from "react-native";
import AppbarHeader from "./AppbarHeader";

const Statistics = () => {
  return <AppbarHeader title="Statistics" />;
};

const styles = StyleSheet.create({
  container: {},
});

export default Statistics;
