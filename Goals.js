import * as React from "react";
import { StyleSheet } from "react-native";
import AppbarHeader from "./AppbarHeader";

const Goals = () => {
  return <AppbarHeader title="Goals" />;
};

const styles = StyleSheet.create({
  container: {},
});

export default Goals;
